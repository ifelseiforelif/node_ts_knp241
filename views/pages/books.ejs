<p>Books page</p>
<h1>Books</h1>

<ul>
  <% books.forEach(book => { %>
    <li>
      <%= book.title %> — <%= book.price %>
      <img src="images/<%=book.image%>" width="200" height="150" alt="тут малюнки"/>
    </li>
  <% }) %>
</ul>